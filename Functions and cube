#include <stdio.h>

int cube(int n);

int main(void)
{ int n;
  printf("Enter the value of n= ");
  scanf("%d",&n);
  cube(n);
  printf("\n");
  
  return 0;
}

int cube(int n)
{
    int cube;
    cube=n*n*n;
    printf("cube = %d",cube);
    return cube;
}
